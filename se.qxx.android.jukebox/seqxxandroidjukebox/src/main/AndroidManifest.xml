<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="se.qxx.android.jukebox"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="23" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:allowBackup="false">
        <activity android:name=".activities.FlipperListActivity"
                  android:label="@string/app_name" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.JukeboxPreferenceActivity" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar"></activity>
        <activity android:name=".activities.PlayerPickerActivity" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar"></activity>
        <activity android:name=".activities.SubSelectActivity" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar"></activity>
        <activity android:name=".activities.FlipperActivity" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar"></activity>
        <activity android:name=".activities.NowPlayingActivity" android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".activities.SeriesActivity" android:theme="@android:style/ThemeOverlay.Material.Dark.ActionBar"></activity>

    </application>
</manifest>